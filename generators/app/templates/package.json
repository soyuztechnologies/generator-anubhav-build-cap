{
  "name": "anubhav-build-app",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^7",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^1",
    "@sap/cds-dk": "^7"
  },
  "scripts": {
    "start": "cds watch",
    "deploy": "npm install && mbt build && cd mta_archives && cf deploy <%= projectName %>_1.0.0.mtar"
  },
  "cds": {
    "requires": {"auth":{
      "kind": "dummy"
    }}
  }
}
